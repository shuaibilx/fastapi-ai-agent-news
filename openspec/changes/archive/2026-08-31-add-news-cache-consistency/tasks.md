## 1. 缓存契约与配置

- [x] 1.1 为新闻分类 generation、新闻分类分页 generation、版本化响应 Key、查询形状、TTL 抖动与空结果 TTL 定义配置和缓存键构造器，并通过单元测试验证同一输入稳定、不同 generation/页码/页大小隔离。
- [x] 1.2 定义可序列化的新闻分页缓存响应模型（`list`、`total`、`hasMore`），并通过单元测试验证空结果和非空结果均可往返序列化。

## 2. 版本化 Cache-Aside 读取

- [x] 2.1 重构分类缓存读取/写入为带 generation 且无分页歧义的缓存策略；验证不同查询形状不会复用错误分类结果，并在 Redis 异常时回源数据库。
- [x] 2.2 重构新闻列表读取为 `publish_time DESC, id DESC` 的稳定分页查询，并以完整分页响应执行版本化 Cache-Aside；验证缓存命中不再额外查询总数、缓存 miss 返回同一快照的列表/总数/`hasMore`。
- [x] 2.3 为空分页写入短 TTL 缓存、为正常缓存应用受控 TTL 抖动；验证重复空页读取不会重复命中数据库，且缓存 TTL 保持在配置范围内。
- [x] 2.4 调整新闻列表路由以消费分页响应模型，同时验证现有成功响应字段和前端分页契约保持兼容。

## 3. 写后失效与运维入口

- [x] 3.1 实现新闻与分类缓存失效协调器：数据库成功提交后递增受影响 generation；验证新增、删除、排序字段更新和分类迁移使旧任意页 Key 不再可读，数据库提交失败不改变 generation。
- [x] 3.2 将现有高频 `views` 更新明确保持为不递增新闻列表 generation 的最终一致字段，并通过测试验证其不会失效整个分类分页缓存。
- [x] 3.3 提供受控的新闻/分类缓存失效运维入口，供导入或手工 SQL 修改后执行；验证其可使已存在的分类及分页缓存不可读，且不会影响摘要、向量索引或 Agent checkpoint 命名空间。
- [x] 3.4 在 Redis 读取、写入或失效失败时记录可诊断日志并保持 MySQL 回源/已提交写入可用；通过故障注入测试验证不向新闻读取接口传播 Redis 基础设施异常。

## 4. 文档与端到端验证

- [x] 4.1 更新 README 或运维文档，说明稳定排序、缓存世代、浏览量最终一致性、导入后缓存失效命令，以及直接 SQL 不会被自动监听的边界。
- [x] 4.2 运行后端完整测试套件与 OpenSpec 严格校验，验证新闻新增/迁移/删除后的分页读取、Redis 降级、空分页缓存和运维失效流程；记录执行结果（`137 passed, 2 skipped`；`openspec validate add-news-cache-consistency --strict` 通过）。
